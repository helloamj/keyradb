# Web Dashboard

The `web` module provides an interactive visualizer for the KeyraDB LSM Engine.

It contains an HTML, CSS, and JS based web application to interact with the database, visualize the memory state, and monitor the metrics.
